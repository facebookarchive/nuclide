/**
 * Header comment.
 */

jest
  .dontMock('foo')
  .dontMock('bar')
  .dontMock('baz');

const one = require('one');
const two = require('two');

describe('foo-bar-baz', () => {
  it('should work', () => {
    expect(one()).toEqual(two());
  });
});
